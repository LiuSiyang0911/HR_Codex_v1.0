<UserControl x:Class="HR_Codex_v0.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:HR_Codex_v0.ViewModels"
             Background="{StaticResource BgBrush}">
    <UserControl.DataContext>
        <vm:ConnectionViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="UDP 连接设置" Grid.Row="0" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="本机 IP" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding LocalIp}" Grid.Row="0" Grid.Column="1" Margin="8,4"/>
                    <Label Content="本机端口" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Margin="16,0,0,0"/>
                    <TextBox Text="{Binding LocalPort}" Grid.Row="0" Grid.Column="3" Margin="8,4"/>

                    <Label Content="远程 IP" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding RemoteIp}" Grid.Row="1" Grid.Column="1" Margin="8,4"/>
                    <Label Content="远程端口" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="16,0,0,0"/>
                    <TextBox Text="{Binding RemotePort}" Grid.Row="1" Grid.Column="3" Margin="8,4"/>
                </Grid>
                <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                    <Button Content="连接" Style="{StaticResource PrimaryButton}" Command="{Binding ConnectCommand}" Width="100" Margin="0,4"/>
                    <Button Content="断开" Style="{StaticResource DangerButton}" Command="{Binding DisconnectCommand}" Width="100" Margin="0,4"/>
                    <Button Content="本机测试" Style="{StaticResource SecondaryButton}" Command="{Binding LocalTestCommand}" Width="100" Margin="0,4"/>
                    <Button Content="交换端口" Style="{StaticResource SecondaryButton}" Command="{Binding ExchangePortCommand}" Width="100" Margin="0,4"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Header="运行日志" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox Grid.Row="0"
                         Text="{Binding LogText, Mode=OneWay}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalAlignment="Stretch"
                         VerticalContentAlignment="Top"
                         HorizontalScrollBarVisibility="Disabled"
                         VerticalScrollBarVisibility="Auto"
                         Background="{StaticResource NavBgBrush}"
                         FontFamily="{StaticResource DataFont}"
                         FontSize="12"
                         Padding="10"
                         Height="Auto"
                         MinHeight="360"/>
                <Button Grid.Row="1" Content="清空日志" Style="{StaticResource SecondaryButton}" 
                        Command="{Binding ClearLogCommand}" HorizontalAlignment="Right" Margin="0,8,0,0"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>

